<Window x:Class="SnippetManager.SnippetEditor"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SnippetManager"
        mc:Ignorable="d"
        Title="SnippetEditor" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="400*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <Button x:Name="buttonSave" Content="Save" HorizontalAlignment="Right" Grid.Row="2" VerticalAlignment="Center" Click="buttonSave_Click" Padding="5,5,5,5" FontSize="16" Margin="0,0,100,0"/>
        <Button x:Name="buttonCancel" Content="Cancel" HorizontalAlignment="Right" Grid.Row="2" VerticalAlignment="Center" Click="buttonCancel_Click" Padding="5,5,5,5" FontSize="16" Margin="0,0,20,0"/>
        <TextBox x:Name="textBoxDescription" Margin="0,0,0,0" Grid.Row="0" TextWrapping="Wrap" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" FontSize="16" />
        <TextBox x:Name="textBoxSnippet" Margin="0,0,0,0" Grid.Row="1" TextWrapping="Wrap" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" FontSize="16" />
    </Grid>
</Window>
